import TariffPage from "../view/TariffPage/TariffPage";
import classes from "./App.module.scss";

const App = () => {
  return (
    <div className={classes.app}>
      <TariffPage />
    </div>
  );
};

export default App;
